customCommands:
  - key: <c-g>
    description: Pick LLM commit
    loadingText: "waiting for LLM to generate commit messages..."
    command: ~/.config/lazygit/commit-oracle/commit-oracle.sh
    context: files
    subprocess: true
